
//Determine if the a string has all unique characters 

public static boolean uniqueString(String s){

  Hashset <Character> set = new Hashset<Character>();
  
      for(int i = 0; i<s.length; i++){
        if(set.contains(S.charAt(i)){
        set.add(S.charAt(i);
        }
        return true;
       }

} 

