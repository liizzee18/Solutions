
smallOnTop(Stack<Integer> s) {
		Stack<Integer> s2= new Stack<Integer>();
		
		int t=s.pop();
		
		while(!s2.isEmpty() && s2.peek() > t) {
			s.push(s2.pop());
		}
		s2.push(t);
	}




I - Sort a stack such that the smallest items are on the top. Can use an additional temporary stack,
    but you may not copy the elements into any other data structure.

D - 


E -


A -


L -


